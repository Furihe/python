mydict = {}

for i in range(2):
    date = input("날짜를 입력하시오: ")
    schedule = input("일정을 입력하시오: ")

  if date in mydict:
        mydict[date].append(schedule)
    else:
        mydict[date] = []
        mydict[date].append(schedule)

print(mydict)
